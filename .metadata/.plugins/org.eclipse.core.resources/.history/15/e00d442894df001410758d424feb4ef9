
public class Solution {
	public void merge(int  A[], int m, int B[], int n) {
		int[] temp = new int[m + n];
		int x = 0;
		int y = 0;
		for (int i = 0; i < m + n; i++) {
			if (A[x] <= B[y]) {
				temp[i] = A[x];
				x++;
			} else {
				
			}
			if (x == m-1) {
				
				break;
			}
			if (y == n-1) {
				break;
			}
		}
	}
	
	public void copyArray(int[] src, int srcOffSet, int[] dst, int dstOffSet) {
		for (int i=0; i<src.length; i++) {
			dst[dstOffSet + i] = src[srcOffSet + i];
		}
	}
}
