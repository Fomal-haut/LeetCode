public class Solution {
	public boolean[] store;
	public boolean canJump(int[] nums) {
		store = new boolean[nums.length];
		store[0] = true;
		for (int i=0; i< nums.length; i++) {
			if (i!=0 && store[i] == true) {
				continue;
			}
			int index = i;
			while (index <nums.length) {
				store[index]
			}
		}
		return store[nums.length - 1];
	}
}
